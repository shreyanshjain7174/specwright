# ── Required ──────────────────────────────────────────────────────────
# Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Cloudflare Workers AI API key (for spec generation + simulation)
CLOUDFLARE_API_KEY=your-workers-ai-api-key

# ── Optional ──────────────────────────────────────────────────────────
# PageIndex API key (for reasoning-based document retrieval)
# Get yours at https://dash.pageindex.ai/api-keys — free tier includes 1000 pages
PAGEINDEX_API_KEY=your-pageindex-api-key

# Base URL for the deployed app (set in Docker/self-hosted environments)
# Vercel sets VERCEL_URL automatically; only needed for Docker/custom deployments
NEXT_PUBLIC_APP_URL=http://localhost:3000
