# Required: Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Required: Cloudflare Workers AI API key (for spec generation)
CLOUDFLARE_API_KEY=your-workers-ai-api-key

# Optional: PageIndex API key (for reasoning-based document retrieval)
# Get yours at https://dash.pageindex.ai/api-keys — free tier includes 1000 pages
PAGEINDEX_API_KEY=your-pageindex-api-key
